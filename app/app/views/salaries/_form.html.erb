<%= form_with(model: salary, local: true) do |form| %>
  <% if salary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(salary.errors.count, "error") %> prohibited this salary from being saved:</h2>

      <ul>
      <% salary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :salary_name %>
  </div>

  <div class="field">
    <%= form.label :wage %>
    <%= form.number_field :wage, id: :salary_wage %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
